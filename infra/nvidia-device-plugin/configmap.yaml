apiVersion: v1
kind: ConfigMap
metadata:
  name: nvidia-device-plugin-config
  namespace: kube-system
data:
  config.yaml: |
    version: v1
    flags:
      migStrategy: none
    resources:
      gpus:
        - pattern: "*"
          name: nvidia.com/gpu
    sharing:
      timeSlicing: {}
