apiVersion: v1
kind: Secret
metadata:
  name: additional-scrape-configs
  namespace: monitoring
type: Opaque
 stringData:
    scrape-configs.yaml: |
      # No external scrape configs — mac-mini scrapes all non-K8s targets directly.
