apiVersion: v1
kind: Secret
metadata:
  name: ghcr-actions-runner-controller-charts
  namespace: default
  labels:
    argocd.argoproj.io/secret-type: repository
type: Opaque
stringData:
  type: helm
  url: oci://ghcr.io/actions/actions-runner-controller-charts
  enableOCI: "true"
  # For ghcr.io public registries, use GitHub username and PAT with read:packages scope
  # Or leave empty for anonymous access (may still require auth for some endpoints)
  username: ""
  password: ""

