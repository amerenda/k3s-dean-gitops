apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: do-dns-api-key
  namespace: default
  annotations:
    argocd.argoproj.io/sync-wave: "10"  # Very high sync wave - after cert-manager is fully bootstrapped
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-secretstore
    kind: ClusterSecretStore
  target:
    name: do-dns-api-key
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        api-key: "{{ .do_dns_api_key }}"
  data:
  - secretKey: do_dns_api_key
    remoteRef:
      key: do-dns-api-key
      property: api-key
