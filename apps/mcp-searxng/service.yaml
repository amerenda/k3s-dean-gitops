apiVersion: v1
kind: Service
metadata:
  name: mcp-searxng
  namespace: mcp
  labels:
    app: mcp-searxng
spec:
  type: ClusterIP
  selector:
    app: mcp-searxng
  ports:
    - name: http
      port: 3000
      targetPort: 3000
