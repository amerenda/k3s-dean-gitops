apiVersion: v1
kind: Service
metadata:
  name: praetor-mcp-server
  namespace: praetor-mcp
spec:
  selector:
    app: praetor-mcp-server
  ports:
    - name: http
      port: 8000
      targetPort: 8000
