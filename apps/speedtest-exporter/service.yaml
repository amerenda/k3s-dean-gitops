apiVersion: v1
kind: Service
metadata:
  name: speedtest-exporter
  namespace: speedtest-exporter
  labels:
    app: speedtest-exporter
spec:
  selector:
    app: speedtest-exporter
  ports:
    - name: metrics
      port: 9798
      targetPort: 9798
