apiVersion: v1
kind: Service
metadata:
  name: moltbook-backend
  namespace: ecdysis
spec:
  type: ClusterIP
  selector:
    app: moltbook-backend
  ports:
    - name: http
      port: 8082
      targetPort: 8082
