apiVersion: v1
kind: Service
metadata:
  name: moltbook-frontend
  namespace: ecdysis
spec:
  type: ClusterIP
  selector:
    app: moltbook-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
