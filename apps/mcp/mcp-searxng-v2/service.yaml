apiVersion: v1
kind: Service
metadata:
  name: mcp-searxng-v2-server
  namespace: mcp-mcp-searxng-v2
spec:
  selector:
    app: mcp-searxng-v2-server
  ports:
    - name: http
      port: 8000
      targetPort: 8000
