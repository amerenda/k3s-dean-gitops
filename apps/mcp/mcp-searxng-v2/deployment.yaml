apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-searxng-v2-server
  namespace: mcp-mcp-searxng-v2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-searxng-v2-server
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: mcp-searxng-v2-server
    spec:
      containers:
        - name: server
          image: ghcr.io/mrkrino/mcp-server-searxng:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
