apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubernetes-readonly-server
  namespace: mcp-kubernetes-readonly
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubernetes-readonly-server
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: kubernetes-readonly-server
    spec:
      containers:
        - name: server
          image: ghcr.io/anthropics/mcp-server-kubernetes:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
