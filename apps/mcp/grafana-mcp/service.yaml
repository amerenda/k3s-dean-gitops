apiVersion: v1
kind: Service
metadata:
  name: grafana-mcp-server
  namespace: mcp-grafana-mcp
spec:
  selector:
    app: grafana-mcp-server
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
