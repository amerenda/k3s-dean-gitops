apiVersion: v1
kind: Service
metadata:
  name: mcp-server-searxng-server
  namespace: mcp-mcp-server-searxng
spec:
  selector:
    app: mcp-server-searxng-server
  ports:
    - name: http
      port: 8000
      targetPort: 8000
