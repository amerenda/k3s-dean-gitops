apiVersion: v1
kind: Service
metadata:
  name: mcp-k8s-server-server
  namespace: mcp-mcp-k8s-server
spec:
  selector:
    app: mcp-k8s-server-server
  ports:
    - name: http
      port: 8000
      targetPort: 8000
