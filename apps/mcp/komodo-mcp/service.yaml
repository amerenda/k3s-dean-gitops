apiVersion: v1
kind: Service
metadata:
  name: komodo-mcp-server
  namespace: mcp-komodo-mcp
spec:
  selector:
    app: komodo-mcp-server
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
