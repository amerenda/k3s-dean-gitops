apiVersion: v1
kind: Service
metadata:
  name: secure-search-mcp-server
  namespace: secure-search-mcp
spec:
  selector:
    app: secure-search-mcp-server
  ports:
    - name: http
      port: 8000
      targetPort: 8000
