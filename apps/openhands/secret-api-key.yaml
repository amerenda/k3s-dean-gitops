---
# Placeholder — the init container patches this with the real API key on startup.
# ArgoCD is configured to ignore diffs on this Secret's data (root-app.yaml).
apiVersion: v1
kind: Secret
metadata:
  name: openhands-api-key
  namespace: openhands
stringData:
  api-key: "pending-registration"
