apiVersion: v1
kind: Service
metadata:
  name: phase0-test-backend
  namespace: phase0-test
spec:
  selector:
    app: phase0-test-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
