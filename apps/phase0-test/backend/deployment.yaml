apiVersion: apps/v1
kind: Deployment
metadata:
  name: phase0-test-backend
  namespace: phase0-test
spec:
  replicas: 2
  selector:
    matchLabels:
      app: phase0-test-backend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: phase0-test-backend
    spec:
      containers:
        - name: backend
          image: amerenda/phase0-test:backend-latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
