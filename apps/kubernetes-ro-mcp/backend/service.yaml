apiVersion: v1
kind: Service
metadata:
  name: kubernetes-ro-mcp-backend
  namespace: kubernetes-ro-mcp
spec:
  selector:
    app: kubernetes-ro-mcp-backend
  ports:
    - name: http
      port: 8000
      targetPort: 3000
