apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubernetes-ro-mcp-backend-uat
  namespace: kubernetes-ro-mcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubernetes-ro-mcp-backend-uat
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 0
  template:
    metadata:
      labels:
        app: kubernetes-ro-mcp-backend-uat
    spec:
      containers:
        - name: backend
          image: amerenda/kubernetes-ro-mcp:backend-latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: ENVIRONMENT
              value: "uat"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
