apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-manager-frontend-uat
  namespace: llm-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: llm-manager-frontend-uat
  template:
    metadata:
      labels:
        app: llm-manager-frontend-uat
    spec:
      containers:
        - name: frontend
          image: amerenda/llm-manager:frontend-sha-807c098
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          volumeMounts:
            - name: nginx-conf
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: default.conf
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 128Mi
      volumes:
        - name: nginx-conf
          configMap:
            name: llm-manager-frontend-uat-nginx
