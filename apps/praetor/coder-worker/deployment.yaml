apiVersion: apps/v1
kind: Deployment
metadata:
  name: praetor-coder-worker
  namespace: praetor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: praetor-coder-worker
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: praetor-coder-worker
    spec:
      nodeSelector:
        kubernetes.io/arch: amd64
      serviceAccountName: praetor-coder
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: coder-worker
          image: amerenda/praetor-coder:sha-5dd4bfb
          imagePullPolicy: Always
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            runAsNonRoot: true
          env:
            - name: HATCHET_CLIENT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: praetor-coder-secrets
                  key: hatchet-token
            - name: HATCHET_CLIENT_TLS_STRATEGY
              value: "insecure"
            - name: MEM0_BASE_URL
              value: "https://mem0.amer.dev"
            - name: MEM0_API_KEY
              valueFrom:
                secretKeyRef:
                  name: praetor-coder-secrets
                  key: mem0-api-key
            - name: VIKUNJA_TOKEN
              valueFrom:
                secretKeyRef:
                  name: praetor-coder-secrets
                  key: vikunja-token
            - name: VIKUNJA_BASE_URL
              value: "https://todo.amer.dev"
            - name: LITELLM_BASE_URL
              value: "https://litellm.amer.dev/v1"
            - name: LITELLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: praetor-coder-secrets
                  key: litellm-api-key
            - name: LLM_MODEL
              value: "coder"
            - name: CODER_APP_ID
              valueFrom:
                secretKeyRef:
                  name: praetor-coder-secrets
                  key: coder-app-id
            - name: CODER_APP_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: praetor-coder-secrets
                  key: coder-private-key
            - name: CODER_APP_INSTALLATION_ID
              valueFrom:
                secretKeyRef:
                  name: praetor-coder-secrets
                  key: coder-installation-id
            - name: SCRATCH_DIR
              value: "/scratch"
            - name: LITELLM_MCP_URL
              value: "https://litellm.amer.dev/mcp/"
            - name: LANGFUSE_HOST
              value: "https://langfuse.amer.dev"
            - name: LANGFUSE_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: praetor-coder-secrets
                  key: langfuse-public-key
            - name: LANGFUSE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: praetor-coder-secrets
                  key: langfuse-secret-key
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: praetor-coder-secrets
                  key: db-password
            - name: PRAETOR_DB_URL
              value: "postgresql://praetor:$(DB_PASSWORD)@agent-kb.amer.dev:5432/praetor"
          volumeMounts:
            - name: scratch
              mountPath: /scratch
            - name: home
              mountPath: /home/appuser
          ports:
            - name: metrics
              containerPort: 9095
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: 2000m
              memory: 4Gi
      volumes:
        - name: scratch
          emptyDir:
            sizeLimit: 4Gi
        - name: home
          emptyDir: {}
