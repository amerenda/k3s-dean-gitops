apiVersion: apps/v1
kind: Deployment
metadata:
  name: praetor-phase25-canary-worker
  namespace: praetor
spec:
  replicas: 0
  selector:
    matchLabels:
      app: praetor-phase25-canary-worker
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: praetor-phase25-canary-worker
    spec:
      nodeSelector:
        kubernetes.io/arch: amd64
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: phase25-canary-worker
          image: amerenda/praetor-phase25-canary:sha-e2356be
          imagePullPolicy: Always
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            runAsNonRoot: true
          env:
            - name: HATCHET_CLIENT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: praetor-phase25-canary-secrets
                  key: hatchet-token
            - name: HATCHET_CLIENT_TLS_STRATEGY
              value: "none"
            - name: MEM0_BASE_URL
              value: "https://mem0.amer.dev"
            - name: MEM0_API_KEY
              valueFrom:
                secretKeyRef:
                  name: praetor-phase25-canary-secrets
                  key: mem0-api-key
            - name: VIKUNJA_TOKEN
              valueFrom:
                secretKeyRef:
                  name: praetor-phase25-canary-secrets
                  key: vikunja-token
            - name: VIKUNJA_BASE_URL
              value: "https://todo.amer.dev"
            - name: LITELLM_BASE_URL
              value: "https://litellm.amer.dev/v1"
            - name: LITELLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: praetor-phase25-canary-secrets
                  key: litellm-api-key
            - name: LLM_MODEL
              value: "phase25-canary"
            - name: SCRATCH_DIR
              value: "/scratch"
            - name: LITELLM_MCP_URL
              value: "https://litellm.amer.dev/mcp/"
            - name: LANGFUSE_HOST
              value: "https://langfuse.amer.dev"
            - name: LANGFUSE_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: praetor-phase25-canary-secrets
                  key: langfuse-public-key
            - name: LANGFUSE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: praetor-phase25-canary-secrets
                  key: langfuse-secret-key
          volumeMounts:
            - name: scratch
              mountPath: /scratch
            - name: home
              mountPath: /home/appuser
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: 2000m
              memory: 4Gi
      volumes:
        - name: scratch
          emptyDir:
            sizeLimit: 4Gi
        - name: home
          emptyDir: {}
