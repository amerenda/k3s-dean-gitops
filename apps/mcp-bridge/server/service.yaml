apiVersion: v1
kind: Service
metadata:
  name: mcp-bridge-server
  namespace: mcp-bridge
spec:
  selector:
    app: mcp-bridge-server
  ports:
    - name: http
      port: 8000
      targetPort: 8000
