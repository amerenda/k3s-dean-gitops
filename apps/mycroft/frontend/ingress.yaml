apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mycroft-frontend
  namespace: mycroft
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.middlewares: default-tailscale-only@kubernetescrd
spec:
  tls:
    - hosts:
        - mycroft.amer.dev
      secretName: mycroft-frontend-tls
  rules:
    - host: mycroft.amer.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mycroft-frontend
                port:
                  number: 80
