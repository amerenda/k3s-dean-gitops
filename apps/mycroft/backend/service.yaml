apiVersion: v1
kind: Service
metadata:
  name: mycroft-backend
  namespace: mycroft
  labels:
    app: mycroft-backend
spec:
  type: ClusterIP
  selector:
    app: mycroft-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
