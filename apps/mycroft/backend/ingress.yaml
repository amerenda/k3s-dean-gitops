apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mycroft-backend
  namespace: mycroft
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - mycroft-api.amer.dev
      secretName: mycroft-api-tls
  rules:
    - host: mycroft-api.amer.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mycroft-backend
                port:
                  number: 8080
