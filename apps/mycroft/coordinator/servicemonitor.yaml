apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: mycroft-coordinator
  namespace: mycroft
  labels:
    app: mycroft-coordinator
spec:
  selector:
    matchLabels:
      app: mycroft-coordinator
  namespaceSelector:
    matchNames:
      - mycroft
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
