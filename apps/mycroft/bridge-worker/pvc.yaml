apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mycroft-bridge-worker-workspace
  namespace: mycroft
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
