apiVersion: v1
kind: Service
metadata:
  name: github-mcp-server
  namespace: github-mcp
spec:
  selector:
    app: github-mcp-server
  ports:
    - name: http
      port: 8000
      targetPort: 8000
